# ── Core v6-v9 dependencies ─────────────────────────────────
pyswip
sympy>=1.12.0
pydantic
langgraph
google-generativeai
google-cloud-aiplatform
google-cloud-storage

# ── v10 NEW: Multi-LLM Peer Review ───────────────────────────
# Mistral API endpoint reviewer
requests>=2.31.0
mistralai>=1.0.0            # v11: updated for structured JSON output

# Gwen/Gemma via HuggingFace (open source reviewer)
huggingface_hub>=0.22.0
# Optional: local vLLM serving for Gwen
# vllm>=0.4.0  # uncomment for local GPU inference

# ── v10 NEW: Lean 4 Proof Cache ──────────────────────────────
redis>=5.0.0

# ── v11 NEW: Hypothesis Validator (DeepProbLog + SymPy) ──────
problog>=2.1.0              # DeepProbLog probabilistic logic
# sympy already included above

# ── v11 NEW: Differential Privacy (DP-FedAvg) ────────────────
opacus>=1.4.0               # Meta Opacus DP training (RDP accountant)

# ── v11 NEW: TensorRT INT8/FP8 Preconditioner ─────────────────
# tensorrt>=8.6.0           # NVIDIA TensorRT (install from NVIDIA, not PyPI)
# cupy-cuda12x>=13.0.0      # cuBLAS INT8 GEMM (uncomment for CUDA 12.x)

# ── v10 NEW: Explainability (Component 8) ────────────────────
shap>=0.44.0
pysr>=0.18.0
lime>=0.2.0                 # v11: LIME fallback when SHAP is too slow

# ── v10 NEW: Federated Research (Component 6) ────────────────
flwr>=1.7.0

# ── Compute & Plotting ───────────────────────────────────────
numpy>=1.26.0
matplotlib>=3.8.0
scipy>=1.12.0

# ── v10 Component 4: cuSPARSE + AMGX ─────────────────────────
pyamg>=5.0.0                 # AMGX-compatible algebraic multigrid (CPU)
# cupy-cuda12x>=13.0.0       # GPU sparse ops — uncomment for CUDA 12.x

# ── v10 Component 6: Flower Federated Learning ───────────────
# flwr already included above
# flwr[simulation]>=1.7.0   # for multi-process simulation

# ── v10 Component 7: PPO RL Agent ────────────────────────────
stable-baselines3>=2.3.0
gymnasium>=0.29.0

# ── v10 Component 8: SHAP + PySR Explainability ──────────────
# shap, lime, pysr already included above
scikit-learn>=1.4.0           # SHAP TreeExplainer + GBM model
# To install Julia for PySR: julia --eval 'using Pkg; Pkg.add("SymbolicRegression")'

# ── v10 SLURM / GCP Backend ──────────────────────────────────
google-cloud-aiplatform>=1.50.0
