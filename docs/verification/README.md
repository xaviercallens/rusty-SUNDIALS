# Verification Certificates

This directory stores the formal trust certificates generated dynamically from the Lean 4 proofs and the test suite coverage.

Certificates map each `rust` module to its corresponding `lean` specification file.
