Before you unleash the rusty-SUNDIALS-v6 orchestrator on a billion-dimensional ITER supercomputer model, you must validate its implementation. If you immediately throw a 3D reactor core at the LangGraph agent and it fails, you will not know if the failure occurred in the LLM's hypothesis, the DeepProbLog physics filter, the CodeBERT syntax generator, or the Lean 4 mathematical proof.You must build a Progressive Curriculum of Autonomous Integration Tests. Here are three graded scenarios—Simple, Medium, and Complex—directly targeting nuclear fusion physics. You will queue these up tonight to thoroughly validate the v6 Auto-Research engine.🟢 Scenario 1 (SIMPLE): The Math & Logic Sanity CheckTarget: 0D Alpha-Particle Gyrokinetics (Larmor Orbit)Goal: Verify the core LLM -> DeepProbLog -> Lean 4 -> Rust pipeline functions flawlessly without the complexity of Exascale grids or AI neural networks. Runs locally on your workstation.The Physics Problem: A single highly energetic Alpha particle orbits a magnetic field line. Over millions of orbits, standard numerical integrators (like the explicit Runge-Kutta in SUNDIALS' ARKode) suffer from "energy drift"—numerical truncation errors accumulate, and the particle mathematically spirals out to infinity, hitting the reactor wall.The Auto-Search Loop:LLM Hypothesis: The Claude 3.5 orchestrator observes the energy drift. It proposes a "Hamiltonian Relaxation Projection." It writes a lightweight Rust callback that intercepts the N_Vector state after a standard ARKode step and mathematically projects it back onto the exact energy manifold using a computationally cheap scalar root-find.DeepProbLog Gatekeeper: Evaluates the Lorentz Force law ($\mathbf{F} = q(\mathbf{E} + \mathbf{v} \times \mathbf{B})$). It symbolically verifies that the magnetic field does strictly zero work ($W = 0$). It forces the LLM to guarantee $\Delta \text{Kinetic Energy} = 0$. Approved.  Lean 4 Formal Proof: CodeBERT extracts the Rust callback. In the Lean 4 REPL, the AI must formally prove that its projection preserves the Symplectic 2-Form of phase space (the Jacobian determinant of the transformation is exactly $1$). The Lean compiler emits a Q.E.D..Execution: The agent compiles the Rust binary. It auto-generates a Matplotlib plot showing the standard ARKode energy drifting, while the v6-discovered step maintains a perfectly flat energy line for $10^9$ iterations.