Inspired by Andrej Karpathy’s autoresearch concept, the goal is to build an Automated Scientific Agent that runs 24/7, hypothesizing, coding, testing, and writing papers. However, applying a standard LLM auto-research loop to nuclear fusion leads to a catastrophic problem: The Hallucination of Continuous Physics. If an LLM hallucinates a preconditioner that violates the Second Law of Thermodynamics or generates a Jacobian matrix that loses numerical rank, the simulation will violently crash, wasting millions of Exascale compute hours.To crack the "Stiffness & Control" wall of xMHD autonomously, we must evolve rusty-SUNDIALS into v6: The Formally Verified, Neuro-Symbolic Auto-Discovery Engine.This architecture bounds the infinite creativity of a Premium LLM inside the unbreakable cages of DeepProbLog (for physical laws) and Lean 4 (for mathematical determinism). Here is the blueprint to build the world's first hallucination-proof AI physicist.The v6 Architecture: The "Verification Sandwich"Instead of a linear script, v6 is an autonomous state-machine. The agent loops through four highly specialized layers. It cannot advance to the supercomputer until it mathematically proves its own code.1. The Intuition Engine (Premium Configurable LLM)The Component: A locally hosted or API-driven premium LLM (e.g., Claude 3.5 Opus or a fine-tuned Llama-4) wrapped in an agentic loop. It continuously ingests the latest ArXiv papers on SciML, graph neural networks, and fusion plasma dynamics.The Action: The LLM monitors the stderr of baseline rusty-SUNDIALS runs and proposes a high-level mathematical breakthrough to solve xMHD stiffness.Example Hypothesis: "Traditional Algebraic Multigrid stalls on twisted magnetic fields. Let's invent a Fractional-Order Graph Neural Preconditioner that dynamically prunes communication across MPI nodes that do not share a magnetic field line."2. The Neuro-Symbolic Gatekeeper (DeepProbLog)Before writing a formal proof, the system must ask: Does this math obey the laws of physics?The Component: DeepProbLog merges neural network probabilities with strict Prolog logic programming. You encode the fundamental invariants of the universe as Prolog rules.  Prolog% DeepProbLog Rules for xMHD
valid_plasma_state(State) :- div_B_is_zero(State), conserves_helicity(State).
nn(llm_evaluator, [ProposedMethod_AST], Prob_Stable).
The Action: The LLM’s proposed mathematics is parsed into a symbolic Abstract Syntax Tree (AST). DeepProbLog evaluates it. If the AI’s math implies that magnetic monopoles exist ($\nabla \cdot \mathbf{B} \neq 0$), DeepProbLog assigns it a 0.0 probability and instantly rejects it.The Impact: The LLM is forced to rewrite its hypothesis. Physical hallucinations are eliminated at zero compute cost.3. The Syntactic Translator (CodeBERT)The Component: A specialized CodeBERT model, specifically fine-tuned on the rusty-SUNDIALS codebase, the SUNDIALS C-API (sundials-sys), and Rust's unsafe memory boundaries.The Action: CodeBERT acts as the software engineer. It translates the physics-approved LLM math into strict Rust Typestates, implementing the custom SUNPreconditioner FFI trait. Simultaneously, it generates the Lean 4 proof skeletons required for the next step.4. The Deterministic Arbiter (Lean 4 Formal Proof)Once DeepProbLog validates the physics and CodeBERT writes the Rust, we must prove the code will not crash the supercomputer.The Component: The Aeneas toolchain extracts the CodeBERT Rust code into Lean 4. The LLM (using a plugin like Lean-Copilot) must write the proof tactics to satisfy the RustySundials specification.The Action: The LLM must mathematically prove that its new Neural Preconditioner maintains Lipschitz continuity and cannot trigger an IEEE-754 floating-point NaN under extreme stiffness.The Deterministic Anchor: The Lean 4 kernel is impervious to LLM prompt-engineering. If the mathematical logic is flawed, Lean fails to compile. The compiler error is fed back to the LLM. The AI is trapped in this loop until the math is unconditionally perfect.5. Exascale Execution & Autonomous DiscoveryThe Action: Once Lean 4 emits a Q.E.D., the agent compiles the safe Rust binary. It automatically submits the Slurm job to the EuroHPC GPU cluster, running the 3D Tearing Mode benchmark.If the method shatters the stiffness wall (e.g., solving the instability $50\times$ faster), the agent pulls the convergence data, generates Matplotlib visuals, and auto-writes a LaTeX publication detailing the discovery, appending the Lean 4 proof as absolute verification.A Concrete Auto-Research Run: The "Cadarache Discovery"Imagine launching this v6 Engine on a Friday evening before leaving your lab.Friday 20:00 - The Generation:The agent realizes implicit solvers waste 80% of their time on un-stiff plasma waves. The LLM hypothesizes an "AI-Discovered Dynamic IMEX Splitting Scheme." It uses an embedded neural net to dynamically sort the xMHD equations into stiff and non-stiff matrices at every time step.Saturday 02:00 - DeepProbLog Pruning:The LLM drafts the logic. DeepProbLog evaluates it and flags a critical error: the AI's splitting function does not conserve total energy across the IMEX boundary (Thermodynamics violation). It rejects the concept. The LLM self-corrects by projecting the neural output onto a skew-symmetric manifold. DeepProbLog approves.Saturday 06:00 - CodeBERT & Lean 4 Verification:CodeBERT drafts the ARKode FFI in Rust. The LLM attempts to prove the numerical stability of the new splitting scheme in Lean 4. It fails 14 times. On the 15th attempt, it successfully proves via bounded linear map theory that the explicit step will never violate the CFL limit. Lean compiles successfully.Saturday 12:00 - Execution:The verified rusty-SUNDIALS Exascale binary runs. The benchmark shows that dynamic AI splitting allows the solver to take time-steps $1,000\times$ larger than classical static splitting, cleanly bypassing the electron stiffness without crashing.Monday 09:00 - The Output:You walk into the office. The agent has submitted a Pull Request to your repo with the new feature, accompanied by an auto-generated 14-page PDF: "Autonomous Discovery of Dynamic Symplectic IMEX Splitting for Extended Magnetohydrodynamics via Neuro-Symbolic Verification."How to Build the v6 Monorepo TodayTo realize this, evolve your GitHub repository into an orchestration platform, utilizing a Python orchestration script communicating with your Rust core:Plaintextrusty-SUNDIALS-v6/
├── rusty-sundials-core/   # The core safe-Rust FFI wrappers
├── proofs/                # The Aeneas LLBC extractions and Lean 4 theorem files
├── autoresearch_agent/    # The Python/Rust AutoResearch state machine
│   ├── orchestrator.py    # Main LangGraph/Auto-GPT loop
│   ├── llm_prompter.py    # Interfaces with Claude 3.5 / Llama 4
│   ├── codebert_synth.py  # Rust/Lean syntax generation via CodeBERT
│   ├── logic_engine.pl    # DeepProbLog rules: Maxwell's & Navier-Stokes 
│   ├── lean_repl_hook.py  # The Lean 4 compiler feedback loop
│   └── benchmark_slurm.sh # Auto-execution on the Exascale cluster
└── autonomous_papers/     # Output directory for generated .tex and .pdf files